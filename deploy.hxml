--dce full

--library iterators
--library haxe-strings
--library safety
--library compiletime

--macro Safety.safeApi("base", true)
--macro nullSafety("base", Strict)

--class-path src
--main JSMain

--js bin/out.js
