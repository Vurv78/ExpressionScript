
GlobalVar = 400

function doStuff() {
	local TestVar = 5

	local Six = TestVar + 1
	TestVar++

	assert(TestVar == Six, "what?")
}

while(1) {
	print("while true do end")
}

Var = 500
switch (Var) {
	case 500,
		print("this will happen")
	break

	default,
		print("this won't")
	break
}